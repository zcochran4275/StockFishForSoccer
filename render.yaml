services:
  - type: web
    name: StockFishForSoccer
    runtime: python
    buildCommand: git submodule update --init --recursive
    startCommand: uvicorn app:app --host=0.0.0.0 --port=10000
    branch: main
    region: oregon